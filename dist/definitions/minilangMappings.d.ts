import { Token } from "./tokens";
/**
 *
 * KEYWORDS:
 * e.g.
 * var while do
 * done if then
 * else endif
 * float int string
 * print read
 *
 *
 */
export declare const KeywordsTokenMapping: {
    [inputStr: string]: Token;
};
